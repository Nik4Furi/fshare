<main class="container my-3" style="min-height: 80vh">
    <% if(messages.error) {%>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <h4 class="text-dark fw-bold fs-6 py-2 px-1 my-2 mx-auto text-center"><%= messages.error%></h4>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
      
    <%}%>
    <% if(messages.success) {%>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <h4 class="text-dark fw-bold fs-6 py-2 px-1 my-2 mx-auto text-center"><%= messages.success%></h4>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
    <%}%>
    <h1>
        <span class="text-primary">Uploads</span> Your Files To Share With Your Buddy 😤
    </h1>
    <p class="lead"><span class="text-primary fw-bold">fshare</span>-A online file sharing app, can help you to, when
        uploads the files and after sharing get the links(of downloading) ,Copy the link and share with anyone</p>
    <p class="lead">Also can email the person to share the file downloading link</p>

    <!-- Upload file component  -->
    <%- include('../partials/UploadFile.ejs')%>

    <!-- Copy upload link  -->
    <%- include('../partials/copyLink.ejs')%>


</main>